<%= form_for(@other_sales_invoice_header, :url => add_discount_put_other_sales_invoice_header_path(@other_sales_invoice_header), :remote => true, :method => :put, :html => { :class => "form-horizontal" }) do |f| %>
  <% if @other_sales_invoice_header.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@other_sales_invoice_header.errors.count, "error") %> prohibited this sales_invoice_header from being saved:</h2>

      <ul>
      <% @other_sales_invoice_header.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :discount, "#{t 'promo_disc'}", :class => "control-label" %>
    <div class="controls"><%= f.text_field :discount, :style => "text-align:right" %></div>
  </div>
  <div class="control-group">
    <%= f.label :discount_amount, "#{t 'disc_rp'}", :class => "control-label" %>
    <div class="controls"><%= f.text_field :discount_amount, :style => "text-align:right" %></div>
  </div>
  <div class="control-group">
    <%= f.label :tax, "#{t 'tax'}", :class => "control-label" %>
    <div class="controls"><%= f.check_box :tax, {:disabled => false}, true, false %></div>
  </div>
  <div class="control-group">
    <div class="controls"><%= f.submit "#{t 'submit'}", :class => "btn" %></div>
  </div>
<% end %>