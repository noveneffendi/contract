$("#note_modal").dialog({
  autoOpen: true,
  height: 300,
  width: 500,
  modal: true,
  title: "<%= "#{t 'notes'} #{@sales_quotation_header.sq_id}" %>",
  open: function() {
    $(this).html("<%= escape_javascript(render('note')) %>")
  },
});