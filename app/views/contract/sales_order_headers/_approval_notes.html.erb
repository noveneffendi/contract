<%= form_for(@sales_order_header, :url => approval_status_sales_order_header_path(@sales_order_header), :method => :put, :html => { :class => "form-horizontal" }) do |f| %>
  <% if @sales_order_header.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sales_order_header.errors.count, "error") %> prohibited this sales_order_header from being saved:</h2>

      <ul>
      <% @sales_order_header.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :approval_notes, :class => "control-label" %>
    <div class="controls"><%= f.text_area :approval_notes, :rows => "7", :required => true %></div>
  </div>
  <div class="control-group">
    <div class="controls">
    	<%= hidden_field_tag "status", @status %>
    	<%= f.submit "#{t 'submit'}", :class => "btn" %>
    </div>
  </div>
<% end %>