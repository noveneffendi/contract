$("#note_modal").dialog({
  autoOpen: true,
  height: 300,
  width: 500,
  modal: true,
  title: "<%= "#{t 'notes'} #{@sales_invoice_header.si_id}" %>",
  open: function() {
    $(this).html("<%= escape_javascript(render('note')) %>")
  },
});