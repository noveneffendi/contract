$("#form_discount_modal").dialog({
  autoOpen: true,
  height: "auto",
  width: "auto",
  modal: true,
  title: 'Discounts',
  open: function() {
    $(this).html("<%= escape_javascript(render('form_discount')) %>")
  },
});